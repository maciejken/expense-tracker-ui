export enum InputType {
  Button = "button",
  Checkbox = "checkbox",
  Date = "date",
  Number = "number",
  Password = "password",
  Radio = "radio",
  Submit = "submit",
  Text = "text",
}

export enum Status {
  Failed = "failed",
  Idle = "idle",
  Loading = "loading",
  ShouldUpdate = "should_update",
}
