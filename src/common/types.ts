export enum InputType {
  Text = "text",
  Number = "number",
  Submit = "submit",
  Password = "password",
  Button = "button",
  Date = "date",
}

export enum Status {
  Idle = "idle",
  Loading = "loading",
  Failed = "failed"
}
