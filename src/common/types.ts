export enum InputType {
  Button = "button",
  Date = "date",
  Number = "number",
  Password = "password",
  Radio = "radio",
  Submit = "submit",
  Text = "text",
}

export enum Status {
  Failed = "failed",
  Idle = "idle",
  Loading = "loading",
  Pending = "pending",
}
